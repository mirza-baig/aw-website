// Generic chart component is not coming to XMC, but still on the site.
// Add an empty component so we stop getting build errors for missing component.

function GenericChart() {
  return null;
}

export const Default = GenericChart;
